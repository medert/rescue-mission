<h1>Questions</h1>

<ul> <% @questions.each do |question| %>
  <li> <%= link_to question.title, question %>"  created by <%= question.creator_id %> ( <%= question.updated_at %> ) </li>
  <% end %>
</ul>

<%= render 'new' %>
